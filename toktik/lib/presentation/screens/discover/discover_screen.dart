
import 'package:flutter/material.dart';

class DiscoverScrean extends StatelessWidget {
  const DiscoverScrean({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
     body:  Center(
     child: Text('discoveri screan'),
     ),
    );
  }
}